name: Release to Maven Central

# Publish is pure Gradle via the Sonatype Central Portal (com.gradleup.nmcp), like
# aresstack/msdos-game-browser, but on Windows so the native node.exe launcher (MinGW) is
# built into the published jar. Triggered by a `v*` tag; version is derived from the tag.
#
# Release gate = the fast, reliable Chrome + Edge channel smokes (launch -> page -> evaluate
# -> close). The full upstream compatibility suite is NOT a blocking gate here — it runs
# separately as a non-blocking audit (see known-limitations-check.yml).
#
# Org secrets used: CENTRAL_USERNAME, CENTRAL_PASSWORD, GPG_PRIVATE_KEY, GPG_PASSPHRASE.
on:
  push:
    tags:
      - 'v*'

jobs:
  publish:
    runs-on: windows-2022
    timeout-minutes: 30
    permissions:
      contents: read

    env:
      BROWSER: chromium
      PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: "1"
      PLAYWRIGHT_SKIP_VALIDATE_HOST_REQUIREMENTS: "1"

    steps:
      - name: Check out sources
        uses: actions/checkout@v4

      - name: Set up Java 21 (Temurin)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Derive version from tag
        id: ver
        shell: bash
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Verify native node.exe launcher builds (fail if MinGW missing)
        shell: pwsh
        run: |
          ./gradlew.bat :playwright4j:compileNodeLauncher --no-daemon --stacktrace
          $exe = "playwright4j/build/generated/node-launcher/com/aresstack/playwright4j/launcher/node.exe"
          if (-not (Test-Path $exe)) {
            Write-Error "node.exe launcher was not built (MinGW gcc missing on the runner); the published jar would lack launch-server support."
            exit 1
          }

      - name: Release gate — Chrome smoke
        shell: pwsh
        env:
          BROWSER_CHANNEL: chrome
        run: ./gradlew.bat :playwright-java-contract-tests:channelSmoke --no-daemon --stacktrace

      - name: Release gate — Edge smoke
        shell: pwsh
        env:
          BROWSER_CHANNEL: msedge
        run: ./gradlew.bat :playwright-java-contract-tests:channelSmoke --rerun-tasks --no-daemon --stacktrace

      - name: Sanity — assemble publishable artifacts locally
        shell: pwsh
        run: ./gradlew.bat :playwright4j:publishToMavenLocal "-Pversion=${{ steps.ver.outputs.version }}" --no-daemon --stacktrace

      - name: Build + sign + publish to Sonatype Central Portal
        shell: pwsh
        env:
          ORG_GRADLE_PROJECT_centralUsername: ${{ secrets.CENTRAL_USERNAME }}
          ORG_GRADLE_PROJECT_centralPassword: ${{ secrets.CENTRAL_PASSWORD }}
          ORG_GRADLE_PROJECT_signingInMemoryKey: ${{ secrets.GPG_PRIVATE_KEY }}
          ORG_GRADLE_PROJECT_signingInMemoryKeyPassword: ${{ secrets.GPG_PASSPHRASE }}
        run: ./gradlew.bat "-Pversion=${{ steps.ver.outputs.version }}" --no-daemon -x test publishAggregationToCentralPortal
